오랜 기간 보호한 동물(1)

SELECT A.NAME,A.DATETIME
FROM ANIMAL_INS A, (SELECT ANIMAL_ID
            FROM ANIMAL_INS

            MINUS

            SELECT ANIMAL_ID
            FROM ANIMAL_OUTS) B
WHERE A.ANIMAL_ID=B.ANIMAL_ID
ORDER BY A.DATETIME FETCH FIRST 3 ROWS ONLY;
