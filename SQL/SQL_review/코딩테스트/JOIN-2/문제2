보호소에서 중성화한 동물

SELECT B.ANIMAL_ID,B.ANIMAL_TYPE,B.NAME
FROM ANIMAL_OUTS A, (SELECT *
                    FROM ANIMAL_INS
                    WHERE SEX_UPON_INTAKE LIKE '%Intact%') B
WHERE A.ANIMAL_ID=B.ANIMAL_ID
AND A.SEX_UPON_OUTCOME NOT LIKE '%Intact%'
ORDER BY B.ANIMAL_ID; 
